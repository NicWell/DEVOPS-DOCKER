# --- Configuração da Aplicação Flask ---
# Nome do arquivo principal da sua aplicação
FLASK_APP=app.py

# Ambiente de execução. Use 'development' para desenvolvimento e 'production' para produção.
FLASK_ENV=development

# Chave secreta para segurança das sessões. Gere uma chave forte e única.
# Use este comando para gerar uma: python -c 'import secrets; print(secrets.token_hex(24))'
SECRET_KEY=coloque_sua_chave_secreta_aqui

# --- Conexão com o Banco de Dados PostgreSQL ---
# O host do banco de dados.
# Use 'localhost' se estiver mapeando a porta do Docker para sua máquina.
# Use o nome do serviço (ex: 'db') se estiver usando Docker Compose.
DB_HOST=localhost

# Porta padrão do PostgreSQL.
DB_PORT=5432

# O nome do banco de dados a ser utilizado.
DB_NAME=mydatabase

# O usuário para conectar ao banco de dados.
DB_USER=myuser

# A senha para o usuário do banco de dados. (NÃO COLOQUE A SENHA REAL AQUI)
DB_PASSWORD=coloque_sua_senha_do_banco_aqui
PGCLIENTENCODING=utf-8